# Security updates only (proposal §7) — the zero PR limit disables version updates,
# security advisories still open PRs.
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 0
