# vX.Y.Z — [version name]

> A prompt for the AI. Rules: CLAUDE.md (especially rule 10). Context: docs/proposal.md.

## Analysis

What this version solves and why. The starting points, the constraints, what it builds on from the previous version, the open questions (those must be resolved with the human BEFORE the implementation — rule 9).

## Solution design

A concrete description: which files are created/changed, the data and schema shapes, the behavior in error states, what is NOT done (out of the version's scope).

## Tasks

- [ ] ...
- [ ] ...
- [ ] **A diary record for this version in AI_DIARY.md** (rule 26 — written in the moment, not retroactively)
- [ ] **The tests for this proposal added and green** (the mandatory last task — without it the version is not done)

> Definition of done: all the checkboxes ticked + the literal output of `npx tsc --noEmit` and `npm test` in the answer.
