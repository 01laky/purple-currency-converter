// the §5 common sum currency — every conversion is converted to EUR at write time
export const STATS_CURRENCY = 'EUR';

// the shared empty/unavailable placeholder of the card values
export const VALUE_PLACEHOLDER = '—';
