export const LANGUAGE_STORAGE_KEY = 'language';

export const FALLBACK_LANGUAGE = 'en';

// the single hardcoded string of the whole app (§3) — shown when /api/init itself fails
export const INIT_FAILURE_MESSAGE = 'Failed to load application';
